import { keyframes } from "styled-components";
const typingText = keyframes`from{width: 0;} to{width: 180px;}`
const blinkCaret = keyframes`from{border-color: transparent;} to{border-color: rgb(0, 0, 0);}`
export {typingText, blinkCaret};
